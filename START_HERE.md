# 🚀 راهنمای فوری شروع کار

## ❌ مشکل: "Backend اجرا نیست؛ حالت محلی فعال است"

**این یعنی:** سرور Backend در `http://localhost:3001` در حال اجرا نیست.

---

## ✅ راه‌حل: دقیقاً این مراحل را دنبال کنید

### قدم ۱: ترمینال اول (PowerShell) را باز کنید

```powershell
cd D:\rp-furniture
node server/index.js
```

**صبر کنید** تا این پیام را ببینید:

```
══════════════════════════════════════════════════════
  🚀 ERP Backend (SQLite) - کاملاً آفلاین و آماده!
  📡 آدرس: http://localhost:3001
  📁 دیتابیس: D:\rp-furniture\data\erp.db
  🔐 ورود: admin@erp.com / admin123
══════════════════════════════════════════════════════

✅ سرور آماده دریافت درخواست است.
```

**⚠️ این پنجره را نبندید! سرور باید همیشه در حال اجرا بماند.**

---

### قدم ۲: ترمینال دوم (PowerShell جدید) باز کنید

از منوی Start، PowerShell جدید را باز کنید.

```powershell
cd D:\rp-furniture
npm run dev
```

پیغام نمایش داده می‌شود:

```
VITE ready in 500 ms
➜  Local:   http://localhost:5173/
```

---

### قدم ۳: مرورگر را باز کنید

برای **بهترین تجربه**، از مرورگر **Chrome** یا **Edge** استفاده کنید:

```
http://localhost:5173
```

---

### قدم ۴: وارد سیستم شوید

```
ایمیل: admin@erp.com
رمز عبور: admin123
```

---

## 🔍 بررسی اینکه سرور درست کار می‌کند

در مرورگر این آدرس را باز کنید:

```
http://localhost:3001/api/health
```

اگر این پاسخ را گرفتید، سرور سالم است:

```json
{
  "status": "ok",
  "db": "sqlite",
  "uptime": 5.23
}
```

---

## ❓ اگر هنوز کار نمی‌کند

### مشکل ۱: `Error: listen EADDRINUSE: address already in use :::3001`

پورت ۳۰۰۱ از قبل اشغال است. آن را آزاد کنید:

```powershell
# پیدا کردن پروسه‌ی استفاده‌کننده از پورت
netstat -ano | findstr :3001

# بستن پروسه (PID را جایگزین کنید)
taskkill /PID 12345 /F
```

سپس دوباره `node server/index.js` را اجرا کنید.

---

### مشکل ۲: `Cannot find module 'better-sqlite3'`

```powershell
cd D:\rp-furniture
npm install better-sqlite3
```

---

### مشکل ۳: سرور بالا می‌آید ولی frontend هنوز پیام آفلاین می‌دهد

1. در مرورگر **F12** را بزنید (Developer Tools)
2. تب **Console** را باز کنید
3. خطای قرمز را ببینید

اگر خطای `CORS` یا `Failed to fetch` دیدید:
- مطمئن شوید سرور در ترمینال اول هنوز در حال اجراست
- مرورگر را refresh کنید (Ctrl+Shift+R)

---

## 📋 ساختار درست:

```
┌────────────────────────────────────────────┐
│ ترمینال ۱ (همیشه باز)                       │
│ > node server/index.js                      │
│ ✅ Backend روی پورت 3001                    │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│ ترمینال ۲ (همیشه باز)                       │
│ > npm run dev                               │
│ ✅ Frontend روی پورت 5173                   │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│ مرورگر                                       │
│ http://localhost:5173                        │
│ ✅ سیستم در حال استفاده                      │
└────────────────────────────────────────────┘
```

---

## ✅ بعد از اتصال موفق

پیام **"Backend اجرا نیست"** ناپدید می‌شود.
حالا می‌توانید:
- ✅ کالا اضافه/ویرایش/حذف کنید
- ✅ فاکتور صادر کنید
- ✅ مشتری ثبت کنید
- ✅ اقساط مدیریت کنید
- ✅ گزارش‌گیری کنید
- ✅ چاپ فاکتور کنید

همه چیز در دیتابیس `data/erp.db` ذخیره می‌شود.
